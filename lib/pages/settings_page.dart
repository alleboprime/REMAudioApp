import 'package:flutter/material.dart';

class SettingsPage extends Container{
  SettingsPage({super.key});

  @override
  Widget build(BuildContext context){
    return Container(
      color: Colors.black,
      child: Center(child: Text("Settings"),), 
      );
  }

}