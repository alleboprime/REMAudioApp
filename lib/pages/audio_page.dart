import 'package:flutter/material.dart';

class AudioPage extends Container{
  AudioPage({super.key});

  @override
  Widget build(BuildContext context){
    return Container(child: Center(child: Text("Audio"),), color: Colors.green,);
  }

}